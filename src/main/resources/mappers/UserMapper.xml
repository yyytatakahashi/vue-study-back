<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.UserMapper">

    <select id="selectUsersOver50" resultType="com.example.demo.entity.User">
        SELECT * FROM users WHERE age >= 50
    </select>

    <select id="selectAllUsers" resultType="com.example.demo.entity.User">
        SELECT * FROM users
    </select>

    <delete id="deleteUser" parameterType="long">
        DELETE FROM users WHERE id = #{id}
    </delete>

    <update id="updateUser" parameterType="com.example.demo.entity.User">
        UPDATE users
        SET name = #{name}, email = #{email}, age = #{age}, blood = #{blood}
        WHERE id = #{id}
    </update>

    <insert id="insertUser" parameterType="com.example.demo.entity.User">
        INSERT INTO users (name, email, age, blood)
        VALUES (#{name}, #{email}, #{age}, #{blood})
    </insert>

</mapper>
